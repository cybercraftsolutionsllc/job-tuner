import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // We will keep this empty for now to ensure the build passes.
  // If you specifically need the React Compiler, uncomment the lines below:
  /*
  experimental: {
    reactCompiler: true,
  },
  */
};

export default nextConfig;